# Generated by Tools\update-luacheckrc.ps1: the Blizzard globals list
# (~44,000 entries, ~1.4 MB) with Tools\luacheckrc.animods.lua appended.
#
# Not committed for two reasons: it is reproducible from a one-line script, and
# regenerating it after a WoW patch would produce a 44,000-line diff that
# buries every real change in the same commit.
#
# Bootstrap a fresh clone with:
#     .\Tools\update-luacheckrc.ps1
.luacheckrc
